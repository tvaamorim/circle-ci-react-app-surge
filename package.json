{
  "name": "circle-ci-react-app-surge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "surge-deploy": "surge --project ./build/ --domain thiago-1-2-3-react-app.surge.sh",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "surge": "^0.20.1"
  }
}
